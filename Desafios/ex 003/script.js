function iniciar(){
    var num = Number(window.prompt("Digite um numero:"))
    var ant = num - 1
    var suce = num + 1
    alert(`Antes de ${num}, temos o número ${ant}. Depois de ${num}, temos o número ${suce}`)
}
